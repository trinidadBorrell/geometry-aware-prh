Created new function CKNDA that's identical to CKNNA, except instead of computing K nearest neighbors, includes a simliarity cutoff.

Since CKNNA uses x@x^T on normalized x as distance metric and selects for max (excluding self), we can safely set a cutoff metric 0 <= x <= 1.